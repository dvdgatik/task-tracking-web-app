# Task Tracking Web App — Next.js + FastAPI + PostgreSQL (Docker)

# Task Web App

Frontend in **Next.js (React + TypeScript)** and backend in **FastAPI (Python, tipado estricto)**, data persistence in **PostgreSQL** with **Docker Compose**.

## Structure
